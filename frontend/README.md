# Frontend Setup

Install dependencies:

```bash
pnpm install
```

Add environment variables:

```bash
cp .env.example .env
# Update the values in .env
```

Run the application (FE):

```bash
pnpm dev
```
